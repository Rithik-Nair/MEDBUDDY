document.addEventListener("DOMContentLoaded", () => {
    const startButton = document.querySelector(".btn-primary");
    
    startButton.addEventListener("click", () => {
      // Simulate a chat or interaction with the AI
      alert("Chat with the AI agent is starting...");
      // Here, you can initiate a chat or a more complex AI interaction
    });
  });
  